export default class Dataset {
  /**
   * constructor description
   * @param  {any} data
   * @param  {Map} columns
   */
  constructor(data, columns) {
    this.data = data;
    this.columns = columns;
  }
}
